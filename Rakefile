# To use these rake tasks, insert the following into your Rakefile
#
# spec = Gem::Specification.find_by_name 'despecable'
# load "#{spec.gem_dir}/Rakefile"

spec = Gem::Specification.find_by_name 'despecable'
import "#{spec.gem_dir}/lib/tasks/docs.rake"
